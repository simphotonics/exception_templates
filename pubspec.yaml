name: exception_templates

description: Parameterized exception and error templates for Dart. Enables
  throwing and catching exception/error objects that are defined by their type argument.

version: 0.1.3-nullsafety

homepage: https://github.com/simphotonics/exception_templates

environment:
  sdk: '>=2.10.0 <3.0.0'

dependencies: null

dev_dependencies:
  minimal_test: ^0.1.2-nullsafety
  pedantic: ^1.9.2
