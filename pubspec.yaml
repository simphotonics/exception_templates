name: exception_templates

description:
  Provides parameterized exception and error templates.
  Enables throwing specific error objects without the need to define
  exception classes.

version: 0.0.4

homepage: https://github.com/simphotonics/exception_templates

environment:
  sdk: '>=2.7.0 <3.0.0'

dependencies:

dev_dependencies:
  test: ^1.15.3
